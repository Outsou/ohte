object vko1 extends App {
	println("Korkeus?")
	var korkeus = readInt
	
	for (i <- 0 to korkeus) {
	  var tyhjat = "";
	  for (j <- 0 to korkeus - 1 - i)
		  tyhjat += " ";
	  
	  var tahdet = "";
	  for (k <- 0 to i+1)
	    tahdet += "*";
	  
	  println(tyhjat + tahdet + tyhjat)
	} 
}